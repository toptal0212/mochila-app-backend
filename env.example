# Server Configuration
PORT=3000
NODE_ENV=development

# Email Service Configuration
# Options: 'gmail', 'sendgrid', 'mailgun', or 'custom'
# If not set, will use console transport for testing
EMAIL_SERVICE=gmail

# Gmail Configuration (if using Gmail)
# 1. Enable 2-Step Verification on your Gmail account
# 2. Generate an App Password: https://myaccount.google.com/apppasswords
# 3. Use the generated app password (not your regular password)
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password

# SendGrid Configuration (if using SendGrid)
# 1. Sign up at https://sendgrid.com
# 2. Create an API key in Settings > API Keys
# 3. Use the API key here
# SENDGRID_API_KEY=your-sendgrid-api-key

# Mailgun Configuration (if using Mailgun)
# 1. Sign up at https://www.mailgun.com
# 2. Get SMTP credentials from Settings > Sending > SMTP
# MAILGUN_SMTP_USER=your-mailgun-user
# MAILGUN_SMTP_PASSWORD=your-mailgun-password

# Custom SMTP Configuration (if using custom SMTP server)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-smtp-user
# SMTP_PASSWORD=your-smtp-password

# Email From Address
# This will be used as the sender email address
EMAIL_FROM=your-email@gmail.com

# Database Configuration (for local development)
# Get these from Vercel Dashboard → Storage → Your Database → .env.local
# POSTGRES_PRISMA_URL=your-vercel-prisma-url
# POSTGRES_URL_NON_POOLING=your-vercel-non-pooling-url

# API Base URL
# For local development
# API_BASE_URL=http://localhost:3000
# For Vercel production (set in Vercel Dashboard)
# API_BASE_URL=https://mochila-app-backend.vercel.app

# AWS S3 Configuration (for image uploads)
# IMPORTANT: Set USE_S3_STORAGE=true to enable S3 storage
# If false or not set, will use local filesystem (won't work on Vercel!)
USE_S3_STORAGE=true

# Get these from AWS IAM Console
# 1. Go to https://console.aws.amazon.com/iam/
# 2. Create a user with S3 permissions
# 3. Create access keys for that user
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key

# AWS S3 Bucket Configuration
# Your S3 bucket name (must be created first in AWS Console)
AWS_S3_BUCKET=mochila-app-images

# AWS Region where your S3 bucket is located
# Common regions:
#   - ap-northeast-1 (Tokyo)
#   - us-east-1 (N. Virginia)
#   - us-west-2 (Oregon)
#   - eu-west-1 (Ireland)
AWS_REGION=ap-northeast-1

# Frontend URL for CORS
FRONTEND_URL=*

